specVersion: "v1beta"
serviceId: "quoteapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quoteapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
