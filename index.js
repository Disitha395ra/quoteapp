import { registerRootComponent } from "expo";
import App from "./src/App"; // Make sure this is the correct path to your App.js file

// This will ensure the app is registered properly
registerRootComponent(App);
